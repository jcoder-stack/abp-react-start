# ABP React Start — 设计规范

**立场：用 shadcn/ui 的原版设计体系，只换一支蓝，另加后台需要而它没有的几样东西。**

令牌的**真值是 `packages/cli/templates/app-theme.css`**（`examples/starter/src/styles.css` 是它的镜像，两份必须逐字一致）。本文件不复制那些值——复制就会漂移，这一点仓库里有过教训。它解释的是两件事：**我们在原版之外改了什么、为什么**，以及**原版没有覆盖的产品约定**。

| | |
| --- | --- |
| [立场与由来](#立场与由来) | 曾经有一套自定义体系，为什么放弃 |
| [与原版主题的差异](#与原版主题的差异) | 覆盖 3 个、新增 11 个、刻度 2 个 |
| [排版](#排版) | 13px 基线、不能动的 16px、CJK 表头 |
| [颜色与状态](#颜色与状态) | 一支蓝、状态五档、对比度承诺 |
| [密度](#密度) | 表格与表单的行高从哪来 |
| [表单](#表单) | 必填标记、说明位置、查看态、关闭闸门 |
| [表格](#表格) | 本系统的核心界面 |
| [侧栏 / 筛选 / 图标](#侧栏) | |
| [动效](#动效) | 三档时长与降低动效 |
| [营销页](#营销页) | 唯一不按后台规则来的地方 |
| [响应式](#响应式) | |
| [定制层清单](#定制层清单) | 主题里那几条 `data-slot` 规则与各自的理由 |
| [已知空白](#已知空白) | |
| [变更纪律](#变更纪律) | |

---

## 立场与由来

这份文件此前描述的是一套自研设计系统：自定义字重 510/590、逐档负字距、四级 surface 阶梯、三级 border 阶梯、五层模态阴影。它被换掉不是因为不好看，是**账算不过来**。

连续七个 PR 里有五个，修的是那套自定义**自己造出来的**问题：中文字重被取整与字距挤死（源于 510/590 与负字距）、移动端输入框触发 iOS 整页放大（源于把 `--text-base` 改成 14px）、十一档刻度产品只用三档、圆形图标钮规则只命中二十二个里的两个、浅色下三处对比度不合规。

最能说明问题的是中文那块：整整三十三行 `:lang(zh)` 的字重与字距覆盖，**只因为 510/590 和负字距才存在**。原版两样都没有，所以那块是删掉，不是继续维护。

完整的账见 [`docs/theme-spike.md`](docs/theme-spike.md)。其中一条结论值得放在这里：**密度并不需要自定义主题**。同一份五字段表单在两套主题下都是 425px，因为密度由组件选的类决定（`h-9` 控件、`gap-5` 字段、表格声明的 `h-10`），主题层唯一的杠杆是 `--text-sm`。

**要换的是体系，不是颜色。**

## 与原版主题的差异

基准是 `shadcn init` 的 blue preset。浅色 32 个令牌、暗色 31 个，**我们只覆盖其中 3 个**，两个主题都是同样三个。

### 覆盖（3）

| 令牌 | 原版 | 我们 | 为什么 |
| --- | --- | --- | --- |
| `--primary` | 浅 0.488 / 暗 0.424 | 浅 **0.55** / 暗 **0.57** | 强调色恰恰是 preset 预期被替换的那一个。而且方向要对：我们这支**暗色比浅色更亮**，所以同一个值既能当填充（承白字 5.08 / 4.66），也能当前景用在链接、当前项、焦点边框上（对页底 5.08 / 4.24）。原版那支反过来，暗色更深，对页底只剩 2.24:1 |
| `--primary-foreground` | `oklch(0.97 0.014 254.604)` | **纯白** | 配我们的蓝，原版那支带蓝调的近白在暗色下只有 4.28:1，低于按钮文字需要的 4.5；纯白是 4.66 |
| `--ring` | 中性灰 | `var(--primary)` | 原版的灰对底色只有 2.59:1，低于非文本控件要求的 3:1 |

其余全部逐字照抄：背景、卡片、浮层、`secondary`、`muted`、`accent`、`destructive`、边框、输入框、`chart-1..5`、圆角、侧栏各面。

> 焦点指示由两部分组成：`focus-visible:border-ring` 的**实边框**与 `ring-ring/50` 的光晕。**承担对比度的是实边框**——光晕是半透明的，在任何主题下都到不了 3:1（浅色 2.44、暗色 1.35）。别把光晕当成无障碍的那一半。

### 新增（11）

原版没有、而组件在用的语义令牌：`status-success` / `warning` / `error` / `info` / `neutral`、`brand-ink` / `brand-mark` / `brand-spark` / `brand-ink-contrast`、`row-selected`、`primary-hover`。

### 刻度（2）

`--text-sm: 0.8125rem`（13px，后台密度基线，原版 14px）与 `--text-2xs: 0.6875rem`（11px，徽标一类的小字）。**其余字号刻度一律用原版**。

### 字体

`--font-sans` 在原版基础上追加 CJK 字族，位置在 `system-ui` **之后**：汉字在 mac/Windows 上由 `system-ui` 那一跳已解决，这几个名字是给 Linux/容器兜底并钉死 Windows 落点；排到前面反而会在 Inter 加载失败时让拉丁字母去借苹方的字形。

### 明确不做的事

不自定义字重（用原版的 400/500/600）、不逐档设负字距、不另造 surface 与 border 阶梯、不给弹窗换面或换圆角、不做圆形图标钮、不加 `cv01/ss03` 之类的 OpenType 定制。**这些都是上一版踩坑最多的地方。**

## 排版

**13px 是产品基线**（`text-sm`），承担全仓约半数的带字号元素。不必为了「层次」去用相邻档位——UI 里 7% 的字号差本来就不可辨，层次靠字重与颜色。

**`--text-base` 固定 16px，不能动。** shadcn 的输入类原语写 `text-base md:text-sm`，那个 idiom 的全部意义是阈值：**iOS Safari 在字号小于 16px 的输入框获得焦点时会放大整页，而且不会自己缩回**。这一档被改成 14px 时，每个桌面截图都还是对的，而手机上每次聚焦都在放大整页。想要更小的正文档，得另起一个名字，这个名字有主了。

`text-lg`（18px）是「比正文大一档」的标题档：弹窗、抽屉、卡片标题。页标题是 `text-2xl`（24px / 400）。

**表头按文字系统分开给。** 英文表头是 12px 全大写：大写字母的 cap-height 约为字号的 0.72，而混排文本的 x-height 只有 0.52，大写变换补偿掉了那点字号差——这是「11–12px 大写表头」这个西文惯例成立的前提。CJK 没有大小写变换，`uppercase` 是空转，那份补偿也不存在，于是 12px 汉字对着 13px 汉字就是实打实小一号。**中日韩回到 13px、不做大写**，区分交给字重与颜色。

> 这条对**所有**用到「小字号 + 全大写」的位置都成立，不只是表头。

## 颜色与状态

一支蓝，两种用途：`primary` 做填充（按钮、当前页码），也做前景（链接、当前项、焦点边框）。`primary-hover` 是它的悬停态。**不再有 `primary-bright`**——那个令牌当初存在，是因为旧的蓝太浅、当文字时对比度不够；现在这支两用都够。

`destructive` 只给不可逆动作与错误态，绝不装饰。

### 状态五档

`status-success`（启用、已付、健康）、`status-info`（进行中、新建）、`status-warning`（待处理、逾期）、`status-error`（失败、离线）、`status-neutral`（草稿、归档）。**保持这五个含义，把新状态映射进去，不要加第六个。**

徽标是「同色圆点 + 同色文字 + 该色 **15% 淡底**」的药丸。**这五个色的明度是按「文字压在自己的淡底上」反解的，不是按「文字压在卡片上」**——用户读到的是药丸内部那组对比，淡底会把对比度吃掉约 0.8–1.2。

**布尔属性不要用状态徽标。** 徽标的含义是「这条记录当前处于某状态」；「这个角色是否公开」是一个字段值。套上药丸既夸大了它，又逼着为它挑一个说不出理由的颜色——角色页曾因此是蓝色、用户页的「启用」是绿色，同一个「是」字两种颜色。属性用普通文字，`否` 走 `muted-foreground`。

### 对比度承诺

两个主题下，**每一对令牌都实测过**：正文、次要文字、按钮文字、焦点实边框、五个状态药丸的内部对比。脚本读主题文件重算，改动令牌后应当重跑。任何新增的语义色必须同样过一遍——尤其是要压在淡底上的。

## 密度

**表格行高是声明出来的，不是内容撑出来的**：舒适 40px、紧凑 32px，由密度档位直接给 `[&_td]:h-10` / `[&_td]:h-8`，单元格不出纵向内距。

这条比数字本身重要。此前行高来自「内距 + 内容」，于是一行里最高的元素说了算，而最高的元素是行尾那个多数行根本不会去点的菜单钮——同一档密度里，纯文本行 36px、带菜单的行 48px。现在按钮 28px，**按钮适配行高，而不是整表迁就按钮**。

表单的节奏是 **6 / 20**：字段内部（标签 → 说明 → 控件 → 错误）6px，字段之间 20px。1.33 倍的差表达不了分组，间距本身就是分组信号。

控件统一 36px 高（`h-9`）、8px 圆角（`rounded-md`）。

## 表单

完整的设计理由见 [`docs/sheetform-design.md`](docs/sheetform-design.md)，此处只列结论。

- **必填不用红星，标「选填」。** 红在本系统里只表示出错与危险；花在一张还没被碰过的表单上，等于在真出错之前就把它的分量用掉了。而且比例通常是反的——录入表单多数字段必填，标一个「选填」比标四颗红星干净。**这个开关属于表单不属于字段**（`OptionalMarks` context）：查询表单字段天然全选填，逐个标等于每条挂一句废话。
- **说明在控件之上、标签之下。** 控件下方属于错误信息，两者共用一个位置就会在「两者都重要」的那一刻挤掉一个。字段的纵向顺序恒定：标签 → 说明 → 控件 → 错误。
- **占位符教格式，不做默认值。** 预填的 `0` 能通过非空校验，会让必填标记永远不触发，用户还得先删掉它。
- **标题要有宾语。** 新建态「新建图书」；编辑与查看态以**记录本身**领衔，标识放副标题（「1984」+「George Orwell · 1949-06-08」）。副标题只在真有东西可说时才占那一行。
- **查看态是一条记录，不是一张禁用的表单。** 禁用控件能豁免对比度下限，前提正是它**不承载内容**。每个字段渲染成一行键值，装在 `divide-y rounded-lg border bg-card` 里：行高 40px（与表格同源）、键左侧 muted、**值右对齐排成一条竖线**。渲染归字段（只有它知道枚举该显示 label、布尔该显示「是/否」），容器归表单，调用方的写法不变。
- **所有关闭路径收敛到一道闸门。** Esc、右上角 ×、点遮罩、取消按钮——脏了先确认，没脏直接关。分散判断迟早漏掉一条，而漏掉的那条只有用户丢了数据才会被发现。确认框的「放弃」用 destructive 红。
- **Esc 要自己兜一层。** 焦点在文本框里时浏览器会消费掉这个键，而表单一打开就自动聚焦第一个字段——不兜的话，模态最标准的退出键在真实路径上从不生效。

## 表格

表格是这套系统的核心界面。外壳是一张带 1px 边框的卡片，工具条与分页脚都在同一张卡里，各自用发丝线与行网格隔开。

行之间**只用发丝下边框，不用斑马纹**——斑马纹在这套安静的配色里读着吵。这条**在约六列以内成立**；更宽的表里横向追踪会变成主要的阅读错误，禁令不再划算：保留发丝线，改用整行 hover 高亮加主列吸附，而不是回去用斑马纹。

- **表头** — `muted` 底、`muted-foreground` 文字；英文 12px 大写，CJK 13px 不大写。可排序列的 chevron 平时低透明度，当前排序列亮成 `primary`。
- **行** — 13px，高度见[密度](#密度)。hover washes `accent` 约 55% 透明度。
- **选中** — 复选框填 `primary`，选中行走 `row-selected`（一层很淡的 primary，和中性 hover 区分得开）。
- **数字列** — 右对齐 + `tabular-nums`。
- **主列** — 头像/字母块配两行「名称 + 次级标识」，ID 用等宽。
- **行操作** — 每行一个 `⋯` ghost 按钮，28px。

选中行时，工具条左区**就地换成批量操作行**：计数、批量动作（危险项做红色文字链接）、清除。右侧工具保持不动。就地替换让危险的批量操作可见但不主位，也不必多长一条工具条。

**分页**在表尾，与行网格用 1px 边框隔开：总数、每页行数、页码控件，当前页是填充的 `primary`。

## 侧栏

固定 256px，窄视口收成 48px 图标轨。自上而下：工作区切换、`⌘K` 搜索、分组导航、底部固定的用户区。

导航项 13px / 400 —— **与正文同字号同字重**。侧栏靠面与颜色（更沉的底、更淡的前景）与内容区分，不靠字重。只有**当前项**升到 500，并带左侧 2px 指示条与染色图标：shadcn 默认只有一块灰底，在长导航里「我在哪」这个信号太弱。

## 筛选与查询

结构化的、由端点驱动的查询表单（ABP 后台各表）**刻意不用 chip 模式**：字段在一个带标签的网格里，收在筛选面板中，默认折叠、由工具条里的漏斗图标打开。

打开面板时**换掉**范围内搜索框——面板的精确字段取代模糊匹配，一屏两个搜索入口是重复的入口。面板自带「重置 / 查询」行，「查询」是次要按钮，因为工具条唯一的主按钮留给「新建」。面板收起时漏斗保留一个圆点表示筛选仍然生效（并给读屏一个等价计数），否则收起的面板会藏起「这张表正被筛选着」这个事实。

## 图标

一律用 Lucide 的**线性图标**，不用填充或多色字形。继承 `currentColor`，随上下文取色。

- **描边** 2px 为默认；只有 24px 以上的独立图标才降到 1.75–1.5px。一个簇里不混描边宽度。
- **12px** 密集的行内示意：排序 chevron、面包屑分隔。
- **14px** 按钮与 chip **内部**、与文字并排的图标。
- **16px** 独立图标：导航项、图标钮、行尾 `⋯`、输入框装饰。

**每个图标都必须显式给宽高**——没有约束的 SVG 会按自身默认尺寸渲染，把布局撑破。

## 动效

动效只有一个职责：说清楚刚才发生了什么。三档时长，按**什么在动**选，不按它有多重要：

| 档 | 时长 | 什么在动 |
| --- | --- | --- |
| 即时 | 100ms | 贴着指针的浮层：combobox、tooltip、菜单 |
| 控件 | 150ms | 原地不动的控件状态：颜色、边框、焦点环、hover |
| 面 | 200ms | 进出与位移：弹窗、抽屉、侧栏宽度、页签指示条 |

150ms 是默认值，不需要写类——未加限定的 `transition-*` 本来就是它。

缓动按方向分：**进**用 `ease-out`（快起慢收，元素落定），**出**用 `ease-in`（慢起快走，让路而不是赖着）。**宽度只用 `linear`**：侧栏收展时内容会跟着 reflow，任何加速曲线都会让里面的文字肉眼可见地抖——这是唯一一处「正确的缓动是没有性格的那个」。

位移上限 8px、缩放不低于 0.95。**有些东西根本不该动**：表格行不做进出动画（数据变化不是表演，边重排边读是读不了的）；hover 只换颜色不位移；加载态脉冲不透明度，不做横扫微光。

**降低动效偏好是硬要求。** 不用「所有动画时长归零」那记大锤——那会连淡入一起砍掉，界面变成硬切，用户就失去了「刚才发生了什么」这个信号；这条偏好要的是别动，不是别提示。做法是把驱动进出动画的位移/缩放/旋转/模糊变量归位、**只留不透明度**，时长压到 100ms。

## 营销页

落地页是这套系统里唯一**不是后台界面**的地方，规则也不同：大字号、大留白、产品本身就是论据。**下面这些只适用于那里**——把它们带进后台，工具就开始像宣传册。

- **区块靠空间分隔，不用分隔线**：桌面 96px、移动 48px。顶部导航那条发丝底边是例外，它标的是「这条是壳」。
- **展示位**用 `rounded-xl` 面板 + 24px 内距 + 一道发丝边框；截图保持原比例，永不为了对齐网格而裁切——需要裁切的截图就是选错了截图。
- **更新日志**在任何断点都是单列时间线，不变成网格。一列到底才扫得动。
- **页脚**多列链接网格，`muted-foreground`，64px 纵向内距。

## 响应式

| 名称 | 宽度 | 主要变化 |
| --- | --- | --- |
| Desktop-XL | ≥1440px | 完整布局，侧栏 256px |
| Desktop | ≥1280px | 不变；营销页测度封顶 1152px，后台不封顶 |
| Laptop | ≥1024px | 卡片网格 3 列 → 2 列；筛选面板 3 列 → 2 列 |
| Tablet | ≥768px | 侧栏收成 48px 图标轨；筛选面板 → 1 列 |
| Mobile | <768px | 侧栏变抽屉；表格在卡片内横向滚动；分页只留上一页/下一页 |

**触控目标**：768px 以下任何可点的控件保有 ≥44px 的点击高度，**用内距去撑，不要放大控件本身**——放大控件就把这套系统赖以成立的密度丢了。

**表格永不改成卡片流**。它在卡片里横向滚动，主列吸附——扫字段时，记录的身份必须留在屏幕上。

## 定制层清单

主题里只剩六条 `data-slot` 规则。收录标准很严：**只放原版给不了、且帮助信息传达的东西**。观感类的（圆形图标钮、五层模态阴影、给弹窗另换面与圆角）一律不收——那类规则正是上一版踩坑最多的地方。

| 规则 | 为什么 |
| --- | --- |
| 侧栏当前项的 2px 指示条与图标染色 | shadcn 默认只有一块灰底，长导航里「我在哪」的信号太弱 |
| `[data-slot="field"]` 间距 6px | 原语给的 12px 对字段间 16px 差得太近，读不出「哪些属于同一个字段」 |
| `[data-slot="table-cell"]` 纵向内距 0 | 配合声明式行高；内距再加一遍，行高就又交还给内容了 |
| `[data-slot="sheet-content"]` 动画 200ms | 原语是开 500ms / 关 300ms，比产品里第二慢的动画还慢 2.5 倍 |
| CJK 表头回到 13px、不大写 | 见[排版](#排版) |
| `prefers-reduced-motion` | 见[动效](#动效) |

**加新规则前先问**：原版真的给不了吗？它是在帮助信息传达，还是只在调观感？`asChild` 会不会把 `data-slot` 换掉？

## 已知空白

- **图表**只有原版的 `chart-1..5` 五个色，没有更完整的分类色板与图表规范。
- **空态 / 加载态 / 错误态的插图**未定义，组件目前只有纯文字态。
- **Toast 的位置与堆叠**沿用原语，未作规定。
- **打印样式**没有。
- **第三档密度**（舒适 / 紧凑之外）没有设计。
- **文案规范**没有：错误信息的口吻、空态措辞、按钮用动词还是名词、标点。
- **双语排版规则**只处理了字体与表头，没有处理中英混排的空格约定、数字与单位。

## 变更纪律

1. **令牌的真值在主题文件里**，本文件不复制值。要改值先问「这是原版给不了的吗」。
2. 改任何颜色令牌之后**重跑对比度脚本**，两个主题都要过。
3. 新定制规则必须能在[定制层清单](#定制层清单)里写出一行理由；写不出来就别加。
4. 先决定它在**密度**与**语义**上的归属，再决定长相。多数「这看着不对」是归属没想清楚，不是颜色不对。
5. 间距与字重优先于颜色。层次读不出来时，通常是布局的问题。
6. 强调色要稀缺。一屏有两个东西在争「点这里」，其中一个是错的。
7. 空白就写在[已知空白](#已知空白)里。沉默会被当成「已经想过了」。
